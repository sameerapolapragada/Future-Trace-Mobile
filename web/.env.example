# Supabase (Vite — client-safe keys only)
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-anon-key

# Future-Trace BFF — optional in local dev (Vite proxies /api → localhost:3000).
# Use the mobile app at http://localhost:5173 — do NOT browse localhost:3000 (legacy Next.js site).
# Set only for production/staging deploy URL:
# VITE_API_BASE_URL=https://your-future-trace-deploy.vercel.app
